info:
  name: DEP-07
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/depenses?periode=mois"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: periode
      value: mois
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
