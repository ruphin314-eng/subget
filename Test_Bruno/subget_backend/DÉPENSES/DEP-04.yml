info:
  name: DEP-04
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/depenses"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
