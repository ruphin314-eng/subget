info:
  name: SEC-5
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/me"
  headers:
    - name: Origin
      value: http://site-inconnu.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
