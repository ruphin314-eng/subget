info:
  name: DASH-01
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/dashboard?periode=jour"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: periode
      value: jour
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
