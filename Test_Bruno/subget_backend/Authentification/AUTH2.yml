info:
  name: AUTH2
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "nom": "ruphin tsafack",
        "email": "jjean.purdot@test.com",
        "motDePasse": "Motdepasse1!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
