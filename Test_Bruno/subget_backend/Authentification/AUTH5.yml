info:
  name: AUTH5
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "nom": "Test User",
        "email": "pas-un-email",
        "motDePasse": "Motdepasse1!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
