info:
  name: AUTH4
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
