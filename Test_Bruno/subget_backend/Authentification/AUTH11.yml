info:
  name: AUTH11
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/refresh-token"
  body:
    type: text
    data: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
