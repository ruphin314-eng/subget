spring:
  datasource:
    url: jdbc:mysql://localhost:3306/subget_backend?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

# Clé de dev uniquement — jamais utilisée en prod (voir application-prod.yml, pas de défaut là-bas).
jwt:
  secret: dev-only-secret-change-me-in-production-min-32-chars

# Défauts pratiques pour tester en local, sans configuration supplémentaire.
admin:
  seed:
    email: admin@subget.local
    password: ChangeMe123!
    nom: Super Admin
